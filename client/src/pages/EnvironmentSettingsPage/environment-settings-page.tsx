import {APP_ROUTES} from '@src/constants';
import React from 'react';

const EnvironmentSettingsPage = () => (
    <div>{APP_ROUTES.ENVIRONMENT_SETTINGS}</div>
);

export default EnvironmentSettingsPage;
